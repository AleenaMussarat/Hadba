{
  "kind": "collectionType",
  "collectionName": "branches",
  "info": {
    "singularName": "branch",
    "pluralName": "branches",
    "displayName": "Branch / الفرع",
    "description": "A restaurant location, English and Arabic together on one entry / موقع مطعم، الإنجليزية والعربية معًا في إدخال واحد"
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "nameEn": {
      "type": "string",
      "required": true,
      "displayName": "Name (EN) / الاسم (EN)"
    },
    "nameAr": {
      "type": "string",
      "required": true,
      "displayName": "Name (AR) / الاسم (AR)"
    },
    "locationEn": {
      "type": "text",
      "required": true,
      "displayName": "Location (EN) / الموقع (EN)"
    },
    "locationAr": {
      "type": "text",
      "required": true,
      "displayName": "Location (AR) / الموقع (AR)"
    },
    "hoursEn": {
      "type": "string",
      "required": true,
      "displayName": "Hours (EN) / الساعات (EN)"
    },
    "hoursAr": {
      "type": "string",
      "required": true,
      "displayName": "Hours (AR) / الساعات (AR)"
    },
    "image": {
      "type": "media",
      "multiple": false,
      "required": false,
      "allowedTypes": [
        "images"
      ],
      "description": "Recommended 1200×900px (4:3) landscape photo, JPG/WebP. Keep image size under 3MB."
    },
    "mapsLink": {
      "type": "string",
      "required": true,
      "displayName": "Map Link / رابط الخريطة"
    },
    "order": {
      "type": "integer",
      "default": 0,
      "displayName": "Order / الترتيب"
    }
  }
}