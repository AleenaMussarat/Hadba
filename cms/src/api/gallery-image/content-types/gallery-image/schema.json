{
  "kind": "collectionType",
  "collectionName": "gallery_images",
  "info": {
    "singularName": "gallery-image",
    "pluralName": "gallery-images",
    "displayName": "Gallery Image / صورة المعرض",
    "description": "A photo shown in the site's Gallery page / صورة تظهر في صفحة المعرض في الموقع"
  },
  "options": {
    "draftAndPublish": false
  },
  "attributes": {
    "image": {
      "type": "media",
      "multiple": false,
      "required": true,
      "allowedTypes": [
        "images"
      ],
      "displayName": "Image / الصورة",
      "description": "Keep image size under 3MB."
    },
    "captionEn": {
      "type": "string",
      "displayName": "Caption (EN) / الوصف (EN)"
    },
    "captionAr": {
      "type": "string",
      "displayName": "Caption (AR) / الوصف (AR)"
    },
    "order": {
      "type": "integer",
      "default": 0,
      "displayName": "Order / الترتيب"
    }
  }
}