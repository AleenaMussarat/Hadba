# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Strapi CMS (separate project — has its own .gitignore for build/db artifacts)
cms/node_modules
cms/.env
cms/.cache
cms/dist
cms/.strapi
cms/.tmp
cms/public/uploads
# cms/build is intentionally committed — Hostinger builds an inconsistent admin
# bundle, so it's built locally (`npm run build` in cms/) and shipped. Rebuild
# and re-commit cms/build/ after any change to cms/src/admin/**.

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
